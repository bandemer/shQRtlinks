<nav class="navbar navbar-expand-lg bg-primary">
    <div class="container-fluid py-2">
        <h1 class="navbar-brand"><a class="text-white text-decoration-none" href="{{ url('index') }}">🔗 shQRtlinks</a></h1>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">

            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <ul class="navbar-nav mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ url('links') }}">Your links</a>
                    </li>
                </ul>
                <div class="ms-auto me-3 text-white">Logged in as: {{ app.user.email }}</div>
                <a href="{{ url('app_logout') }}" class="btn btn-sm btn-light me-3">Logout</a>
            {% else %}
                <form class="d-flex ms-auto" action="{{ path('app_login') }}" method="post">
                    <input class="form-control me-2" type="text" id="username" name="_username" placeholder="Email">
                    <input class="form-control me-2" type="password" id="password" name="_password" placeholder="Password">
                    <input type="hidden" name="_target_path" value="{{ url('links') }}">
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                    <button class="btn btn-light" type="submit">Login</button>
                </form>
            {% endif %}
        </div>
    </div>
</nav>